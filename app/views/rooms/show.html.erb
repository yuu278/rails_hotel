<h1><%= @room.name %></h1>

<% if @room.image.attached? %>
  <%= image_tag @room.image, alt: "施設画像", class: "room-image" %>
<% else %>
  <%= image_tag "facility_image.png", alt: "デフォルト画像", class: "room-image" %>
<% end %>

<p>紹介文: <%= @room.description %></p>
<p>宿泊料金: ¥<%= @room.price %>/日</p>
<p>住所: <%= @room.address %></p>

<%= link_to '予約一覧', room_reservations_path(@room) %>
<%= link_to "予約する", new_room_reservation_path(@room), class: "btn btn-primary" %>
<%= link_to "戻る", rooms_path, class: "btn btn-secondary" %>